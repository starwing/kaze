[package]
name = "kaze-config"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5.30", features = ["derive"] }
serde = { version = "1.0.218", features = ["derive"] }
toml = "0.8.20"

[dependencies.clap-merge]
path = "../clap-merge"
